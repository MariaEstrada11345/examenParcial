
# Songs Service (SoundBeat)

Ejemplo de microservicio GET /canciones endpoint que retorna canciones del archivo canciones.json

Correr localmente:
```
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python app.py
```

Después abrir http://localhost:5002/canciones
