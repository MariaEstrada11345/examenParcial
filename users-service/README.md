
# Songs Service (SoundBeat)

Ejemplo de microservicio GET /usuarios endpoint que retorna las personas del archivo usuarios.json

Correr localmente:
```
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python app.py
```

Después abrir http://localhost:5001/usuarios
